// Placeholder login page so the route exists for redirects. The real login form
// is built in Phase 8.
export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <div className="text-center">
        <h1 className="text-2xl font-bold">Sign in to EngageDash</h1>
        <p className="text-muted-foreground mt-1">Login form arrives in Phase 8.</p>
      </div>
    </main>
  );
}
