// Placeholder so we can see the app shell. The real dashboard is built in Phase 10.
export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold">Dashboard</h1>
      <p className="text-muted-foreground mt-1">Metrics and charts arrive in Phase 10.</p>
    </div>
  );
}
