# =========================================================================
# EngageDash frontend — environment template
# Copy to `.env.local` for local dev. In Docker, provided via docker-compose.
# =========================================================================

# Base URL of the backend API (no trailing slash).
NEXT_PUBLIC_API_URL=http://localhost:8000
